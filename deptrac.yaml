deptrac:
  paths:
    - ./src
  layers:
    - name: Core
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Core(\\|$)/'
    - name: Application
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Application(\\|$)/'
    - name: Database
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Database(\\|$)/'
    - name: Journal
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Journal(\\|$)/'
    - name: Execution
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Execution(\\|$)/'
    - name: Transport
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Transport(\\|$)/'
    - name: Outcome
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Outcome(\\|$)/'
    - name: Status
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Status(\\|$)/'
    - name: Http
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Http(\\|$)/'
    - name: Logging
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Logging(\\|$)/'
    - name: Console
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Console(\\|$)/'
    - name: Internal
      collectors:
        - type: classNameRegex
          value: '/^BlackOps\\Internal(\\|$)/'
    - name: Library
      collectors:
        - type: classNameRegex
          value: '/^Monolog(\\|$)/'
        - type: classNameRegex
          value: '/^FastRoute(\\|$)/'
        - type: classNameRegex
          value: '/^Doctrine\\DBAL(\\|$)/'
        - type: classNameRegex
          value: '/^Doctrine\\Migrations(\\|$)/'
        - type: classNameRegex
          value: '/^Psr\\Clock(\\|$)/'
        - type: classNameRegex
          value: '/^Psr\\Cache(\\|$)/'
        - type: classNameRegex
          value: '/^Psr\\Container(\\|$)/'
        - type: classNameRegex
          value: '/^Psr\\Http(\\|$)/'
        - type: classNameRegex
          value: '/^Psr\\Log(\\|$)/'
        - type: classNameRegex
          value: '/^Ray\\Aop(\\|$)/'
        - type: classNameRegex
          value: '/^Symfony\\Component\\Console(\\|$)/'
        - type: classNameRegex
          value: '/^Symfony\\Component\\DependencyInjection(\\|$)/'
        - type: classNameRegex
          value: '/^Symfony\\Component\\Stopwatch(\\|$)/'
        - type: classNameRegex
          value: '/^Symfony\\Component\\Uid(\\|$)/'
        - type: classNameRegex
          value: '/^Symfony\\Component\\Validator(\\|$)/'
  ruleset:
    Core: ~
    Application:
      - Core
      - Internal
      - Library
    Database:
      - Core
      - Library
    Journal:
      - Core
    Execution:
      - Core
      - Journal
    Transport:
      - Core
      - Journal
      - Execution
      - Outcome
      - Library
    Outcome:
      - Core
    Status:
      - Core
    Http:
      - Core
      - Execution
      - Library
    Logging:
      - Core
      - Journal
      - Library
    Console:
      - Core
      - Journal
      - Execution
      - Transport
      - Library
    Internal:
      - Core
      - Journal
      - Execution
      - Transport
      - Http
      - Logging
      - Console
      - Outcome
      - Status
      - Database
      - Library
