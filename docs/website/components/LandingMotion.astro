<script>
  import { bootLandingMotion } from '../scripts/landing-motion.mjs';

  bootLandingMotion();
</script>

<style is:global>
  /* The ready marker is set by the module only after it has a target list.
   * Without JavaScript the page keeps its normal, fully visible layout. */
  html[data-landing-motion='ready'] .landing-shell [data-landing-reveal] {
    opacity: 0;
    transform: translate3d(0, 1.5rem, 0) scale(0.985);
    transition:
      opacity 650ms cubic-bezier(0.22, 1, 0.36, 1),
      transform 650ms cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: var(--landing-reveal-delay, 0ms);
    will-change: opacity, transform;
  }

  html[data-landing-motion='ready'] .landing-shell [data-landing-reveal].landing-motion-visible {
    opacity: 1;
    transform: none;
    will-change: auto;
  }

  html[data-landing-motion='ready'] .landing-shell .landing-brand[data-landing-neon-title] .landing-brand__letter {
    opacity: .72;
  }

  html[data-landing-motion='ready'] .landing-shell .landing-brand[data-landing-neon-title].landing-neon-arrival .landing-brand__letter {
    animation: landing-neon-letter-arrival var(--landing-neon-duration, 2.1s) cubic-bezier(0.22, 1, 0.36, 1) var(--landing-neon-delay, 0ms) 1 both;
  }

  html[data-landing-motion='ready'] .landing-shell .landing-brand[data-landing-neon-title].landing-neon-settled .landing-brand__letter {
    opacity: 1;
    text-shadow: none;
  }

  @keyframes landing-neon-letter-arrival {
    0% {
      opacity: .72;
      text-shadow: none;
    }
    8% {
      opacity: 1;
      text-shadow: 0 0 .08em var(--landing-accent), 0 0 .24em color-mix(in srgb, var(--landing-accent) 72%, transparent);
    }
    15% {
      opacity: .5;
      text-shadow: none;
    }
    23% {
      opacity: 1;
      text-shadow: 0 0 .06em var(--landing-accent), 0 0 .2em color-mix(in srgb, var(--landing-accent) 64%, transparent);
    }
    32% {
      opacity: .64;
      text-shadow: none;
    }
    41% {
      opacity: 1;
      text-shadow: 0 0 .05em var(--landing-accent), 0 0 .16em color-mix(in srgb, var(--landing-accent) 56%, transparent);
    }
    53% {
      opacity: .56;
      text-shadow: none;
    }
    62% {
      opacity: 1;
      text-shadow: 0 0 .04em var(--landing-accent), 0 0 .12em color-mix(in srgb, var(--landing-accent) 42%, transparent);
    }
    76%, 100% {
      opacity: 1;
      text-shadow: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    html[data-landing-motion='ready'] .landing-shell [data-landing-reveal],
    html[data-landing-motion='ready'] .landing-shell [data-landing-reveal].landing-motion-visible {
      opacity: 1;
      transform: none;
      transition: none;
      will-change: auto;
    }

    html[data-landing-motion='ready'] .landing-shell .landing-brand[data-landing-neon-title] .landing-brand__letter {
      animation: none;
      opacity: 1;
      text-shadow: none;
    }
  }
</style>
