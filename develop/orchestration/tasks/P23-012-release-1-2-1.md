# P23-012: Release Framework, Skeleton and Documentation1.2.1

Status: In Progress

## Goal and Authorization

The user explicitly requests adapting the accepted work for1.2.1 and completing
the release. This authorizes the necessary candidate commits, branch pushes, PR
creation/merge, annotated Framework/Skeleton publication, GitHub Release,
Packagist propagation and documentation deployment, after the required gates.
Do not ask for the same release authorization again. Immutable published tags
remain immutable; unexpected remote divergence or failed gates stop dependent
publication until resolved. No1.3 product feature is part of this patch.

## Source and Scope

Primary workspace /home/kubotak/projects/blackops is the Orchestrator workspace,
with unreleased1.3 source at70f9c4733bed8bb0960165cddf8c451b78b96184. Preserve its
existing staged/untracked work and unrelated D2S1 Report. Release work occurs in
/tmp/blackops-framework-1.2.1, branch agent/p23-009-framework-1-2-1, maintenance
base5ad87557c08e20c3bcc6d77be83804001ba3eb80. P23-009's19-file version delta is
accepted. P23-008/P23-008A/P23-010/P23-011's approved public-site work must be
integrated against this maintenance implementation before candidate freeze.

Framework PHP delta stays limited to the two accepted Telemetry scope version
constants. No new API, dependencies, schema or1.3 Audit/CLI/runtime source may
enter the release. Public guides must describe the maintenance implementation,
including its current Retention CLI options. Retain the explicitly unpublished
1.3 Audit plan. Existing ^1.2 Skeleton constraint remains compatible.

## Team and File Ownership

Root owns integration decisions, this Task/Report, primary STATE/TODO, copied
management specifications needed by site guards, dependency/container preparation,
shared DB and generated dist, Git/PR/remote operations, all final gates and
Acceptance. All shell commands originate in the primary repository root and
select the maintenance checkout explicitly. Primary current profiles apply.

Site Luna owns maintenance docs/website/** and the public docs/guide/** backport,
including registered diagram PNG assets. Copy only tracked/untracked source files
from the approved primary tree, excluding generated content/dist/node_modules.
Reconcile the four pre-existing primary-vs-maintenance guide differences in
application-bootstrap, project-cli, retention and scheduled-operation against
maintenance PHP. The public Retention wrapper accepts four period flags; the
fifth period comes from configuration. A direct Consumer probe confirmed the
outer definition rejects idempotency-record-days before the inner factory.
Internal commands having five flags does not establish public CLI capability.
Preserve the explicit/ordinary/detailed global-list distinctions, which also
apply to1.2. Version-specific guards must describe the complete public path.
No diagram geometry/design changes.
Use the existing Japanese docs/design guidance. Do not build shared dist or
modify primary sources. Return changed inventory, source audit and scoped tests.

Release Luna independently owns maintenance tests/Consumer/version-baseline.sh,
tests/Consumer/skeleton-create-project.sh and its docs/internal/mvp-e2e.md label,
CHANGELOG.md, UPGRADE.md, root README.md and examples/quickstart/README.md as
needed for1.2.1 preparation. Other already accepted Consumer version changes are
preserved. Update obsolete assertions about the old landing only where replaced
by the accepted presentation contract; retain release/architecture/security guards.
Release Luna also owns the exact accepted test-only5aa82af backport in
tests/Internal/Outbox/OutboxRelayRuntimeTest.php, preserving real heartbeat DB
callbacks and re-arm assertions while removing the fragile2.2-second timing window.
Do not promote public currentStable before actual tags/Packagist publication.
Record a dated1.2.1 changelog for this patch and its compatibility/no-migration
boundary. Coordinate exact landing marker changes with Site Luna. Root may extend
this ownership narrowly if preflight identifies another required release file.

Read-only Documentation Reviewer owns release-order preflight and final all-public
source/artifact review. Workers do not commit, stage, publish or write STATE.
No duplicate shared generation/DB/Consumer execution.

## Release and Documentation Authority

Current published tuple at start: Experimental Stable1.2.0; Framework direct
00e8c5875047a3c47acbebfe57f75b0e581d18b9, peeled3332fd1dd0738fc7e79750facd93d49a59054ecf;
Skeleton directfedcfda5f39caf320ad67196e8ced459176cedb1,
peeledfa5e8247fc8cf789cf73685e5be59cc498ffb4ce. Roadmap1.3.0/unreleased.
Changing capability IDs: framework-core version metadata and documentation-site
presentation. Existing capability since versions are historical and preserved.
Authority spec46/61/103/104 and current workflows govern publication.

The release candidate can carry the new site while installs/currentStable remain
1.2.0 until publication is verified. The immutable1.2.1 tag's own hash cannot be
embedded in its own source. After Framework/Skeleton refs and Packagist are live,
a separately reviewed docs closeout commit updates currentStable and exact refs,
current install commands/notices, source/Search/LLM and historical allowlist as
needed, then passes same-SHA documentation CI and canonical production checks.
No guessed future tag IDs or false pre-publication claim.

## Publication Closeout Ownership

D is prepared on agent/p23-012-release-docs from reviewed main release source
4efee09f13bebedc1639333f79550a36a4e8ca91. Site Luna owns the authority JSON and
its exact historical allowlist in addition to guides/site. Release Luna owns
current root/Quickstart README, Changelog/Upgrade and version-baseline. Local D
preparation may overlap immutable package publication; actual authority refs,
final review and any D push wait for verified Framework/Skeleton/Packagist.

After D is merged and its same-SHA CI succeeds, Root may synchronize Skeleton
main to the deterministic subtree split of D only if its difference from the
published1.2.1 split is README.md alone. Verify two split results, R-split ancestry,
remote-main fast-forward and all remote tag direct/peeled refs before/after.
This is a separate documentation branch update; never reassign1.2.1 or dispatch
the tag-publication workflow for D. Document the existing non-fast-forward
rejection when re-dispatching an older tag after main has advanced. The immutable
release README is a publication-time snapshot; current README is on main.

Root delegates the exclusive published-package remote-smoke command to Release
Luna while Root owns website generation. The frozen /tmp harness6e4de3f9 uses
isolated Composer caches, the verified tag/Packagist refs and pinned Docker image
ab739e1d3bb8d9c7b6696401f7d4821eff72ae870db703feb537015a4fa14f5d
(PHP8.5.7); it owns its unique Compose project and temporary cleanup. Failures are
preserved and diagnosed before any bounded retry. Site source remains frozen
while Root builds; the release worker does not modify source or shared dist.

## Acceptance Criteria

- [x] Candidate contains the accepted presentation/doc changes and only allowed
  Framework patch changes; no1.3 implementation or incorrect current claims.
- [x] All public sources are reviewed against maintenance capabilities, old
  version occurrences classified, and generated HTML/Markdown/Search/LLM correct.
- [x] Fixed committed candidate, clean release checkout, Composer strict,
  Mago format/lint/analyze, PHPUnit/Deptrac, all applicable Consumer E2E,
  frontend, website, package export and deterministic Skeleton/smoke gates pass.
- [x] Same-SHA GitHub CI/documentation evidence is green; reviewed candidate is
  in remote main history through the repository's PR policy before tagging.
- [x] New annotated1.2.1 Framework and Skeleton tags point to the verified
  source/split; existing tags unchanged. Publication workflow succeeds.
- [ ] GitHub Release and both Packagist packages advertise verified1.2.1 refs;
  remote normal/no-scripts install and documented Quickstart are proven.
- [ ] Publication-closeout source and exact authority refs pass independent
  review and same-SHA CI/docs delivery; canonical site, installs, Releases,
  Search and LLM artifacts show current1.2.1 with1.3 still unreleased.
- [ ] Primary work is preserved and synchronized where needed; Report/STATE
  identify final refs, URLs, evidence, remaining issues and Next Action.

## Verification Plan

Release requirements in spec61 require broad qualification; do not substitute
P23-009's preparation-only checks. Run full quality/Consumers once on a frozen
candidate, reusing completed commands only when relevant inputs/environment match.
No extra partial tests immediately before the same full suite. Remote CI and
publication workflow gates are retained. A changed candidate invalidates affected
checks and any same-SHA delivery claim; preserve diagnostic failures and resume
from the necessary boundary, rather than blindly restarting successful prefixes.

Site Luna performs syntax, source/content/reader audits and focused tests needed
to reconcile the backport. Release Luna runs shell syntax/version-baseline only
after site source is stable. Root owns build, final all-source/artifact/browser
checks, serial Consumers/DB and strict quality, candidate commit/export and CI.
Evidence under /tmp/blackops-p23-012 records command, UTC date, inputs/config/lock,
runtime/image, exit/counts/seconds/log and process state. Preserve the accepted
primary preview at4321; build the maintenance dist separately. No raw secrets.

For documentation closeout D, Root runs local typecheck/build and all-public
source/artifact guards. The complete website suite is assigned to D's required
same-SHA GitHub CI/Documentation jobs; do not also repeat the same full suite
locally after focused worker checks. Runtime/PHP inputs remain equal to qualified
R, so no extra local PHP suite is selected for the docs-only diff. Any actual new
failure expands the affected verification. Existing mandatory remote jobs remain.

## Completion

Report: develop/orchestration/reports/P23-012-release-1-2-1.md. Full1.2.1 release
is complete only after package and canonical documentation verification. Root
rotates STATE at start, completion, blocker and session end.
