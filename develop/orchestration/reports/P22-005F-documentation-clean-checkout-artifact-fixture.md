# P22-005F Report: Documentation Clean-Checkout Artifact Fixture

Status: Accepted — Sol xHigh P1=0／P2=0／P3=0

Completed At: 2026-08-18T17:14:54+09:00

Accepted At: 2026-08-18T17:26:05+09:00

## Summary

The bounded PR #10 CI correction is Accepted without a commit. `docs/website/tests/reader-contract.test.mjs` no longer copies the ignored local `docs/website/dist`. The HTML full-artifact test now writes a synthetic complete 40-page artifact from `contentMap` (excluding `README.md`) into its own temporary directory, validates the complete artifact before mutation, and then runs the existing installation-route HTML active/inert/malformed matrix unchanged.

The exact clean-checkout failure was reproduced before the correction: the website suite finished 117/118 with `ENOENT` at `reader-contract.test.mjs:855` while `cp()` attempted to `lstat` `docs/website/dist`. After the correction, the focused synthetic-artifact test passes 1/1 and the complete website suite passes 118/118 while the real `dist` is moved aside and restored by a trap.

No commit, stage, push, CI rerun, merge, deploy, or Production mutation was performed.

## Independent Acceptance Evidence

The Orchestrator independently accepted this exact uncommitted candidate after the final test change. Sol xHigh final read-only review returned P1=0／P2=0／P3=0 and supported P22-005F Local Acceptance.

- Real `docs/website/dist` unavailable via recoverable move plus trap: focused synthetic-artifact test 1/1 PASS; full website suite 118/118 PASS; `dist` restored after each run.
- Release source guard, Website check, fresh build, site check, release artifact guard, generated/public boundary, and `git diff --check`: PASS.
- Accepted current public Artifact hashes retained because P22-005F changed only the test and management documents: `index.html` `1f0128c49c4908f798dfa4fcd8c302dbe2a893c2eeffee922e9347ec3b1d47ef`; `blume-search.json` `dd1968391b3178932b4a1ee4fccb468d2d715222b23d614b0b43d1afabb36fe5`.
- Browser verification was Not Run because no public Source, generated Artifact, route, Search, or visual code changed; the accepted current Artifact hashes and prior parent Browser evidence remain valid. Quickstart, Mago, and PHP management-ID verification were Not Run because this bounded correction is website-test plus management-only. Remote CI rerun, exact Commit, same-SHA Documentation delivery, Deploy, and Production canonical verification were not performed by the P22-005F child task. Parent delivery is User-approved and the Orchestrator will perform it after fixing the reviewed exact commit.

## Changed Files

- `docs/website/tests/reader-contract.test.mjs`
  - Removed the `cp` import and real `docs/website/dist` copy.
  - Added a test-local synthetic-artifact writer for 40 raw Markdown pages, 40 HTML routes, `blume-search.json`, `llms.txt`, and `llms-full.txt`.
  - Renamed the test to `synthetic complete artifact validation applies HTML reader routing to generated pages`.
  - Preserved the existing 37 HTML active/inert/malformed injection cases and their expectations.
- `develop/orchestration/tasks/P22-005F-documentation-clean-checkout-artifact-fixture.md`
- `develop/orchestration/reports/P22-005F-documentation-clean-checkout-artifact-fixture.md`
- `develop/orchestration/tasks/P22-005-documentation-governance-and-information-architecture.md`
- `develop/STATE.md`
- `develop/TODO.md`

Ignored build output under `docs/website/dist` was only moved recoverably for the clean-checkout tests and regenerated by the required fresh build. No tracked public Artifact file changed.

## Decisions and Assumptions

- The synthetic artifact is generated from the actual `contentMap` at test time, so route and outcome drift is caught by the same `validateArtifactReaderContract` used for the generated Artifact.
- `README.md` is excluded because it is the Landing entry and is intentionally outside the 40-page reader inventory.
- Raw Markdown writes `description: ${JSON.stringify(outcome)}` exactly; HTML contains only the mapped escaped outcome as reader content; Search and `llms.txt` contain exactly the 40 mapped routes and outcomes; each `llms-full.txt` segment contains an HTTPS Source URL and the required `blackops-reader-outcome` marker.
- The existing validator and all injection cases remain the contract under test. `reader-contract.mjs`, workflows, package metadata, public Guide, and public Artifact were not changed.
- Browser, Quickstart, Mago, PHP management-ID, CI, Commit, Push, Deploy, and Production verification are outside this bounded website-test correction and are recorded as Not Run below.

## Exact Failure Evidence

Base candidate: `4d755cdb75c9b2f2c4d7eab9ee547bf011042511` on `agent/p22-005-documentation-governance`.

- PR #10 CI Run `32088975752`, Job `95567261225`: reported failure in the full website suite.
- PR #10 CI Run `32088975758`, Job `95567261797`: reported the same clean-checkout contract failure.
- Sol xHigh bounded verdict: P1=1, P2=0, P3=0.
- Reproduction command, with `docs/website/dist` moved to `/tmp/p22-005f-dist-baseline.XXXXXX/dist` and restored by an EXIT trap:

  ```text
  mise exec -- pnpm --dir docs/website run test
  ```

- Reproduction result: `tests 118`, `pass 117`, `fail 1`; test `full artifact validation applies the HTML reader routing to generated pages` failed at line 851, with the `cp()` call at line 855 raising:

  ```text
  Error: ENOENT: no such file or directory, lstat '/home/kubotak/projects/blackops/docs/website/dist'
  ```

## Commands and Results

All commands ran from `/home/kubotak/projects/blackops`. For both clean-checkout test runs, the real `docs/website/dist` was moved to a unique `/tmp` directory, restored by an EXIT trap, and never deleted.

| Command | Result |
| --- | --- |
| `node --check docs/website/tests/reader-contract.test.mjs` | PASS |
| Focused final synthetic-artifact test with real `dist` unavailable | PASS, 1/1; 8.7s |
| Full final `mise exec -- pnpm --dir docs/website run test` with real `dist` unavailable | PASS, 118/118; 11.7s |
| `mise exec -- pnpm --dir docs/website run release:check:source` | PASS |
| `mise exec -- pnpm --dir docs/website run check` | PASS; 41 pages, 0 errors, 0 warnings, 0 hints |
| `mise exec -- pnpm --dir docs/website run build` | PASS; 42 pages built, static Artifact boundary PASS, site check 41 pages |
| `mise exec -- pnpm --dir docs/website run site:check` | PASS; 41 pages |
| `mise exec -- pnpm --dir docs/website run release:check:artifact` | PASS |
| Generated/public boundary guard from the parent CI contract | PASS; no tracked generated content and no `docs/internal`/`develop/` Artifact references |
| `git diff --check` | PASS |
| `git status --short` | PASS; only the six allowed Task/Report/parent/STATE/TODO/test files are changed or untracked |

After each clean-checkout run, `docs/website/dist` was confirmed restored and present (`16M`).

Not Run by scope: Browser harness, Quickstart Consumer, Mago, PHP management-ID scan, remote CI rerun, Commit, Push, Merge, Deploy, and Production canonical verification. The reasons and retained Artifact hashes are recorded under Independent Acceptance Evidence above.

## Release Documentation Impact

- Authority tuple / Capability ID: Stable `1.2.0` and its capability inventory are unchanged; this is a hermetic test-fixture correction only.
- Public Source / route inventory: unchanged at 41 sources and 40 reader routes. No public page, Search, LLM, slug, redirect, or tracked Artifact content changed.
- Version occurrences / historical allowlist: unchanged; no public occurrence was added or removed.
- Source / Search / LLM fixtures: the new synthetic artifact is test-only and covers all 40 route/outcome mappings plus the existing HTML active/inert/malformed cases. It is not a replacement public Artifact.
- Same-SHA CI / Documentation delivery / Production deploy: not performed by the P22-005F child task. Parent delivery is User-approved and the Orchestrator will perform it after fixing the reviewed exact commit.

## Acceptance Criteria

- [x] No `cp` import/use remains, and the test does not read the real `docs/website/dist`.
- [x] A complete 40-page synthetic artifact is generated from `contentMap`; baseline `validateArtifactReaderContract` passes before injection.
- [x] Existing 37 HTML active/inert/malformed cases remain unchanged and pass their expected safe/unsafe outcomes.
- [x] Focused and full clean-checkout runs pass with the real ignored Artifact unavailable; the Artifact is restored by trap.
- [x] Website check, fresh build, site/artifact/release guards, generated/public boundary, diff, and status checks pass.
- [x] Non-applicable Browser/Quickstart/Mago/PHP/remote delivery commands are explicitly recorded Not Run.
- [x] Task Packet, Report, parent, STATE, and TODO are synchronized with exact evidence and remaining work.

## Remaining Issues

P22-005F Remaining Issues: none. Parent delivery actions were not performed by this child task. User has approved the parent delivery; the Orchestrator must first fix the reviewed exact commit, then perform same-SHA CI/Documentation delivery, authorized Production canonical verification, and parent closeout.

## Suggested Next Action

Next Action: Orchestrator fixes the exact accepted snapshot to a reviewed exact commit, then performs the User-approved same-SHA CI/Documentation delivery gates and authorized Production canonical verification under the parent workflow. Do not reuse the pre-fix 117/118 failure as current evidence.
